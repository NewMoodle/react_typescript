import React, {FC} from 'react';
import {observer} from "mobx-react-lite";

const Admin: FC = () => {
    return (
        <div>
            Admin page
        </div>
    );
};

export default observer(Admin);